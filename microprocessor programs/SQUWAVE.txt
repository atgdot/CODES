	OUTPUT 2500AD
	ORD 2000H
	MOV AX,0000H
	MOV CS,AX
	MOV ES,AX
	MOV DX,0FFE6H
	MOV AL,80H
	OUT DX,AL

START: 
	MOV AL,0FFH
	MOV DX,0FFE0H
	OUT DX,AL
	MOV DX,0FFE2H
	OUT DX,AL
	CALL DELAY
	MOV AL,00H
	MOV DX,0FFE0H
	OUT DX,AL
	MOV DX,0FFE2H
	OUT DX,AL
	CALL DELAY
	JMP SHORT START
	DELAY
SS:	LOOP SS
	RET
	END
